# AppSettings
JasonPereira84's applicationSettings library

## Running bash scripts
1. Change ownership of file (Step 1 needs to be run the first-time/once only)
```
sudo chmod +x /path/to/file
```
2. Run the script
```
./BumpVersionNumber.sh <VERSION NUMBER>
```